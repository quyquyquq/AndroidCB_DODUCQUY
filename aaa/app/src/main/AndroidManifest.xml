<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".StartActivity"
            android:exported="true"> <!-- Đặt exported là true cho activity khởi động -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".list_truyencuoi"
            android:exported="false" /> <!-- Đặt exported là false nếu không cần truy cập từ bên ngoài -->

        <activity android:name=".TruyenCuoiDetailActivity"
            android:exported="false" /> <!-- Tương tự -->
    </application>
</manifest>
